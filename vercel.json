{
  "framework": "nextjs",
  "rewrites": [{ "source": "/api/python/:path*", "destination": "/api/python/:path*" }],
  "buildCommand": "npm run build",
  "installCommand": "npm install && pip install -r backend/requirements.txt"
}
